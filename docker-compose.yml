services:
  db.cardapio:
    image: mysql:9
    restart: always
    environment:
        MYSQL_ROOT_PASSWORD: L34ndr0.$1lv@
        MYSQL_DATABASE: cardapio
        MYSQL_USER: usr_cardapio
        MYSQL_PASSWORD: cardapio
    ports:
        - "3306:3306"
    volumes:
      - mysql.cardapio:/var/lib/mysql
volumes:
  mysql.cardapio: